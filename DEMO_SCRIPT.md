# Lifeline Grid — 2:55 Demo Script

Record at desktop width with `OPENAI_API_KEY` configured. Use only the built-in synthetic scenario. The evaluation figures are generated by the app; do not describe them as field results.

## 0:00–0:16 — Hook

**Screen:** Opening command center.

> A blackout can leave critical facilities without power while large batteries sit nearby. Lifeline Grid turns those stranded batteries into a verified emergency grid—without consuming the mobility reserve people still need.

## 0:16–0:37 — GPT-5.6 understands the need

**Action:** Click **Analyze reports with GPT-5.6**.

> Three fictional facilities report needs in natural language. GPT-5.6 converts them into source-linked power contracts: load, duration, deadline, priority, connector, confidence, and assumptions.

Hold briefly on **GPT-5.6 LIVE**.

## 0:37–0:58 — A persuasive plan fails

**Screen:** E-12 → Clinic appears in red.

> The nearest-looking candidate sounds reasonable, but it is unsafe. E-12 cannot cover eight hours and would breach the protected 35 percent mobility reserve. Lifeline blocks it visibly.

## 0:58–1:32 — Exact robust optimization

**Action:** Click **Run robust constraint optimizer**, then scroll just enough to include the benchmark.

> The model does not guess the answer. Deterministic code evaluates all 60 distinct allocations. Every plan is tested across 256 reproducible scenarios spanning demand, battery state, and travel uncertainty—15,360 plan-scenario evaluations. The objective protects priority service first, then reliability, response time, and mobility margin.

> The nearest-feasible baseline fails 49 scenarios. Lifeline’s selected plan succeeds in all 256 and also survives the joint adversarial bound. These are synthetic scenario results, not a real-world claim.

## 1:32–1:50 — Human authority

**Action:** Click **Approve simulated dispatch**.

> A machine-checkable plan still does not authorize action. A human incident lead approves the simulation, and the decision enters the audit timeline.

## 1:50–2:23 — GPT-5.6 changes machine state; the optimizer recovers

**Action:** Click **Interpret bridge closure + re-plan**.

> Then a free-text update says East Bridge is closed. GPT-5.6 turns that narrative into a structured blocked-route event. The rolling mission plan is rebuilt globally: E-21 changes missions, E-44 takes the water station through Ridge Bypass, and all 256 stress scenarios remain safe.

Hold on the event-evidence strip and re-routed map.

## 2:23–2:45 — How it was built

> GPT-5.6 handles the two language problems: incident intake and disruption interpretation. Deterministic software owns physical feasibility and optimization; a human owns authorization. Codex was my primary development collaborator across product architecture, interface, implementation, optimization, tests, evaluation, and this demo workflow. I made the safety policy and final product decisions.

## 2:45–2:55 — Close

> Chat can describe a crisis. Lifeline Grid proves a plan, measures its fragility, keeps a human in command, and adapts when reality changes.

## Recording checklist

- Keep the video at or below three minutes and make it public on YouTube.
- Confirm both model interactions show **GPT-5.6 LIVE**.
- Clearly say the benchmark is synthetic and reproducible, not a field-performance claim.
- Show the red rejection, comparison benchmark, human approval, and global re-plan.
- Mention both Codex and GPT-5.6 in the voiceover.
- Do not imply real deployment or autonomous control.
