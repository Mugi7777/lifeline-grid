# Server-side only. Never expose this value in browser code or commit a real key.
OPENAI_API_KEY=your_openai_api_key_here

# JSON trust registry containing public signing keys only. Never place authority
# private keys in Lifeline Grid. Empty means signed events fail closed.
AUTHORITY_TRUST_REGISTRY_JSON={"keys":[]}
